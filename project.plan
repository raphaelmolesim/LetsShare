Project Plan

Rename the classes Refund, and Payment (Maybe become only one) ==> Done!
The debtors should have some way to pay the creditors ==> Done!
Find a Layout
Create a rails project
Create database and models
==> View changes

Change Person to User
Separate the bills of a user per project

